const user = ["linux", 10, "Torvalds"];

// var role = user[2];
// var name = user[0];

// var [name, courseCount, lastName] = user;


// console.log(lastName);

const myUser = {
        name : "linux", 
        courseCount : 10,
        role : "Admin",
};

// console.log(myUser.courseCount);

const {name, courseCount, role} = myUser;

console.log(courseCount);

